({
	handleClick1 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch1(cmp);
    },
    handleClick2 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch2(cmp);
    },
    /*
    handleClick3 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch3(cmp);
    },*/
    
    handleClick4 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch4(cmp);
    },
    handleClick5 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch5(cmp);
    },
    handleClick6 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch6(cmp);
    },
    
    handleClick8 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch8(cmp);
    },
    handleClick9 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch9(cmp);
    },
    handleClick10 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch10(cmp);
    },
    handleClick11 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch11(cmp);
    },
    handleClick12 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch12(cmp);
    },
    handleClick13 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch13(cmp);
    },
    handleClick14 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch14(cmp);
    },
    handleClick15 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch15(cmp);
    },
    handleClick16 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch16(cmp);
    },
    handleClick17 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch17(cmp);
    },
     handleClick18 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch18(cmp);
    },
     handleClick19 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch19(cmp);
    },
     handleClick20 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch20(cmp);
    },
    handleClick21 : function (cmp, event, helper) {
        alert("The batch " + event.getSource().get("v.label") + " has started");
		helper.lancerBatch21(cmp);
    },
})